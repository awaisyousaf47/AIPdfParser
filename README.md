# AIPdfParser
We built an AI-powered PDF parser that allows users to upload PDF files, converts the content into vector embeddings, and stores them in a vector database. When a user asks a question about the uploaded PDF, the system searches the vector database for the most relevant content and uses it to generate an accurate answer based on the document.
